name: CI
on: [push]
jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and test
        run: xcodebuild -scheme Touch -destination 'platform=macOS,arch=x86_64' build test
